# namste_dev_tutorials


# redux to build the store
- install @reactjs/toolkit react-redux
- build the store 
- connect store to our app
- Slice(slicecart)
- dispatch(action)
- Selector

# testing
- unit testing
  - react testing lbrary install
  - install jest
  - install babel dependencies from jest website
  - setup babel.config.js with the defaults
  - configure parcel config to disable bable default transpilation
  - install jsdom library
  - install testing library/jest-dom